import 'package:flutter/material.dart';

class ChefProjetScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Espace Chef de Projet")),
      body: Center(child: Text("Bienvenue, Chef de Projet !")),
    );
  }
}
