import 'package:flutter/material.dart';

class MembreScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Espace Membre")),
      body: Center(child: Text("Bienvenue, Membre !")),
    );
  }
}
